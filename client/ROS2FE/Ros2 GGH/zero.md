# 目录结构

```text
Ros2 GGH/
├── CLAUDE.md                 # 项目工作约定
├── README.md                 # 项目说明
├── zero.md                   # 更新日志（本文件）
├── interface.md              # ROS2 话题接口契约
├── Design/
│   ├── modern.md             # 现代简约设计规范
│   └── console-hud.md        # 控制台 HUD 设计方案（透明叠印 + 开放式取景框）
├── Backend/                  # C.WebApi 接入交付物（文档 / SDK / Postman）
│   ├── C.WebApi-前端接口与实时通信接入文档.md
│   ├── C.WebApi.postman_collection.json
│   ├── C.WebApi.local.postman_environment.json
│   ├── cwebapi-client.js
│   ├── cwebapi-client.d.ts
│   └── postman/              # Postman Local View 工作区资源
├── Robot/
│   └── 通信脚本/             # 机器人局域网 rosbridge 样例与话题接口
│       ├── video.py          # RGB 原始图像订阅
│       ├── move.py           # 速度发布 / 安全速度订阅
│       ├── keyboardmoving.py # 20Hz 键盘控制
│       ├── interface.md
│       └── 通信接口.md
└── Frontend/                 # Nuxt 4 前端（单页控制台）
    ├── nuxt.config.ts
    ├── package.json
    ├── components.json       # shadcn-vue 配置
    ├── tsconfig.json
    └── app/
        ├── app.vue           # 首页：顶部导航 + 概览/控制台 KeepAlive 视图切换(.zero) + .site-footer
        ├── assets/css/main.css   # Tailwind v4 入口 + 设计令牌（亮/暗主题）
        ├── composables/
        │   ├── useAuth.ts       # 标签页会话恢复、Token 到期与认证 API
        │   ├── useRobot.ts      # 操作者状态/门控 + CommandHub 按需 15Hz 控制循环
        │   ├── useTelemetry.ts  # MapHub 地图/遥测/导航指令、帧频诊断与连接生命周期
        │   ├── useOverviewRuntime.ts # 最近控制台会话的电量 / 链路快照
        │   └── useLanCamera.ts  # rosbridge 摄像头直连、解码与重连
        ├── plugins/signalr.client.ts # 挂载 globalThis.signalR（SDK 运行时）
        ├── lib/
        │   ├── cwebapi/      # 与 Backend/ 同步的 SDK 副本（含 MapHub 帧解码错误回调）
        │   ├── map-snapshot-cache.ts # IndexedDB 静态地图回退快照
        │   ├── whep-player.ts # 内网 WHEP 接收与签发地址归一化
        │   └── utils.ts      # cn() 类名合并工具
        └── components/
            ├── AuthDialog.vue    # 登录 / 注册 / 游客配套认证弹窗
            ├── OverviewView.vue  # 单屏操作席摘要：身份、现场视频、电量与最近会话链路
            ├── OverviewVideoPreview.vue # 概览单一 SRS WHEP 视频预览与帧状态
            ├── ConsoleView.vue   # 控制台 HUD 编排（视频 / 态势 / 感知舞台 + 三模式抽屉）
            ├── console/          # 控制台 HUD 与媒体舞台组件
            │   ├── KeyboardControls.vue # 桌面键盘映射与按压反馈
            │   ├── JoystickPad.vue      # 移动端虚拟摇杆
            │   ├── EStopButton.vue      # 移动端急停按钮
            │   ├── DiagnosticsPanel.vue # 连接、帧频、媒体性能与最近错误
            │   ├── OperatorPanel.vue    # 当前操作权查看、管理员移交与回收
            │   ├── VideoStage.vue       # SRS / Rosbridge / 局域网三来源切换 + FPS
            │   ├── MapStage.vue         # 覆盖范围态势场、原始解析与 RTS 借鉴式单目标交互
            │   ├── PointCloudStage.vue  # PointCloudHub + Three.js 近距感知 / 空间扫描
            │   └── ...           # HudPanel / Vitals / Switcher / PiP / Telemetry
            ├── UiTooltip.vue     # Reka UI Tooltip 统一悬浮提示
            └── ui/button/        # shadcn-vue Button 组件
```

# 更新日志

- **2026-08-07 12:40** 概览主视觉改为单一 SRS WHEP 视频预览：移除概览地图快照读取、IndexedDB 加载与 Canvas 绘制，保留电量和 API/CMD/MAP/VID/PCD 最近会话摘要；概览视频激活时建立、离开时释放，控制台地图/点云仍保留原有链路。同步 README、CLAUDE.md 与现代设计规范；`pnpm typecheck` 通过，新标签页桌面/移动端概览无地图 Canvas、无运行时警告，未执行生产构建或联调。

- **2026-08-07 10:58** 确认后端同账号单会话：同一 `admin` 二次登录后旧 Token 由 `200` 变为 `401`，前端刷新恢复仅在未被其他端重新登录吊销时生效；认证提示补充“其他端登录”说明，并同步 README 与项目约定。

- **2026-08-07 10:53** 完善认证刷新恢复与点云首帧状态：同标签页会话恢复后继续携带 Bearer；PointCloudHub 订阅 3 秒无首帧改为 `HUB · NO FRAME` / 诊断 `NO FRAME`，不再无限转圈或误报登录失效。实测后端当前返回 `default → /points`，Hub 可连但 8 秒无帧，需后端/机器人修正话题配置；同步 README 与项目约定。

- **2026-08-07 10:27** 登录会话改为标签页 `sessionStorage` 恢复：刷新页面保留 Access Token、身份与权限但不保存密码；关闭标签页、登出、过期或损坏数据自动清除。认证客户端新增 `401` 代际保护，旧请求不能清掉后来重新登录的 Token；修复刷新后点云/设备请求无 Bearer Token 而连续 `401`。同步 README 与项目约定；`pnpm typecheck` 通过，未执行生产构建或机器人联调。

- **2026-08-06 22:38** 修复“控制台返回概览”稳定卡死：根视图改为单实例 `KeepAlive` 缓存，避免一次切换集中卸载 GPU、RTC、MapHub 与 CommandHub；点云暂停 rAF/GPU 缓冲消费，地图暂停 Canvas 重绘，视频暂停帧采样，重新进入恢复当前会话。隐藏控制台立即解绑键盘/焦点驾驶输入并停止进行中的手动速度循环；登出、刷新或最终卸载仍完整清理。同步 README、项目约定与 HUD 设计；`pnpm typecheck` 通过，未复现会拖死电脑的实机路径，未执行生产构建或机器人联调。

- **2026-08-06 22:15** 修复点云舞台反复创建 WebGL 上下文导致浏览器卡顿/卡死：正常切换仅释放 Three.js 资源，不再主动丢失上下文；创建、尺寸调整、绘制或上下文事件失败时立即停止 PointCloudHub 与绘制、展示本地 `OFF` 错误，并在 15 秒冷却内拒绝自动重建；渲染像素比上限降为 `1.5`。同步 README、项目约定与 HUD 设计；`pnpm typecheck` 通过，未执行生产构建或机器人联调。

- **2026-08-06 21:42** 修正概览桌面比例：导航与概览统一为 `1440px` 阅读宽度，地图预览改为弹性占用单屏剩余高度，运行摘要底线与场景对齐；宽屏地图 Skeleton 增加高度上限，窄屏保持原有紧凑单列。同步 README、项目约定与设计规范；开发态 `1280×720`、`2048×1095`、`390×844` 无横向/纵向溢出，未执行生产构建或联调。

- **2026-08-06 21:25** 概览最近会话快照改为内存即时更新、`sessionStorage` 最多约每秒合并一次；控制台卸载时落盘最后一帧，退出/注销取消待写数据，避免电量高频遥测重复写存储。同步 README、项目约定与设计规范；`pnpm typecheck` 通过，未执行生产构建或联调。

- **2026-08-06 21:14** 概览压缩为单屏运行摘要：移除工作视角入口和英雄主按钮，地图环境窗口按视口扩展，新增受“后端地址 + 身份”隔离的最近控制台会话电量与 `API / CMD / MAP / VID / PCD` 链路快照（30 分钟有效，明确 `LAST SESSION` 语义）；顶部控制台导航复用认证门控，窄屏补充图标化入口，空页脚不再占用首屏。`pnpm typecheck` 通过，桌面/390px 开发态无滚动或横向溢出，未执行生产构建。

- **2026-08-06 20:37** 概览首版收尾：同步站点页脚注释与目录说明；开发态确认桌面布局、空地图 Skeleton/空状态、状态行悬停 Tooltip、未登录入口认证弹窗与移动端无横向溢出，未执行生产构建或完整联调。

- **2026-08-06 20:01** 完成操作席概览首版：新增身份简报、地图快照预览、进入前检查、视频/态势/感知工作视角入口、固定尺寸 Skeleton 与统一 Reka UI Tooltip；概览不建立实时机器人连接，缓存地图仅作观察上下文。
- **2026-08-06 18:51** 修复态势场首帧空白竞态：态势视图不再等待原始栅格位图，直接按当前 `MapFrame` 绘制范围、网格和状态；全未知帧增加低对比点阵与 `NO ENV EVIDENCE` 场内提示，并抵消 ROS 世界层翻转以保持文字可读。解析视图仍保留原始位图门控，不改地图真值、落点判断或导航。

## 更新记录

- **2026-08-06 17:36** 新增地图 IndexedDB 静态快照与点云会话级可见 LOD 缓冲：未完整连接时分别标记 `CACHED` 回退，地图缓存只读且须等新 Map 帧才可导航，点云不落盘；同步诊断、README、HUD 与项目约定，`pnpm typecheck` 通过。
- **2026-08-06 16:20** 地图态势改为默认开启的独立会话态势场：仅按 `MapFrame` 原点/朝向/尺寸绘制覆盖范围、米制参考网和角标，彻底移除轮廓、墙线与占据碎片推断及 `d3-contour` 依赖；解析、原始栅格、落点阻止与导航调用不变。同步 README、项目约定与 HUD 设计；未执行生产构建或机器人联调。
- **2026-08-06 15:57** 重做地图结构呈现：撤回会拉坏多边形的顶点正交化，默认态势改提取原始占据格中的高置信水平/垂直墙线（`≥0.75m`，仅合并 `≤0.12m` 的厚度/小断差），不再填充粗糙深色结构块。原始栅格、解析视图、导航与机器人坐标不变；同步 README、项目约定与 HUD 设计，`pnpm typecheck` 通过。
- **2026-08-06 15:45** 地图长边正交化：仅对近水平/垂直、长度不少于 `0.30m` 且偏移不超过 `0.36m` 的展示边局部对齐，修复轮廓简化产生的长斜线；斜边、短边、断裂与导航真值不改。同步 README、项目约定与 HUD 设计，`pnpm typecheck` 通过。
- **2026-08-06 15:30** 地图轮廓精修：地面/结构按地图分辨率换算为约 `0.12m` / `0.075m` 简化；结构体改为物理面积或跨度门槛，默认缩放隐藏低权重碎片。只改善展示几何，原始栅格、占据判断与导航调用不变；同步 README、项目约定与 HUD 设计，`pnpm typecheck` 通过。
- **2026-08-06 15:21** 地图行动层收束：计划路径改为蓝色双层路线，即时路线改为青绿标记，历史轨迹降为低对比虚线；机器人新增定位环与朝向引线。宽线仅改善读图层级，不表示安全通道或导航置信度；同步 README、项目约定与 HUD 设计，`pnpm typecheck` 通过。
- **2026-08-06 15:15** 地图态势收束：仅将贴近主可通行区的真实占据连通块显示为结构体，脱离场景的小块降为观测物；移除地图外接矩形底色，以连续地面轮廓建立视觉边界。仅改展示层，不改占据判断、导航调用或机器人通信；同步 README、项目约定与 HUD 设计，`pnpm typecheck` 通过。
- **2026-08-06 15:08** 地图结构化展示层：态势以 Marching Squares、最大连续自由区与占据连通域生成缓存轮廓，解析保留原始栅格；Map 内容未变不重建几何，场景框不再随每个 MapHub 帧重复入场。仅做纯显示，未改占据判断、导航调用或机器人通信；同步 README、项目约定与 HUD 设计，`pnpm typecheck` 通过。
- **2026-08-06 12:27** WHEP 会话隔离：快速切源、换设备、重连或卸载后，旧签发响应、PeerConnection、媒体轨道与 FPS 回调不能清空新画面或覆盖当前 HUD；未新增 SRS 协议、远端控制或机器人请求。同步 README、项目约定与 HUD 设计。
- **2026-08-06 12:06** 统一局域网摄像头默认端口：`useLanCamera` 回退值改为与 Nuxt 运行时一致的 `9091`；README 明确原始机器人样例 `9090` 与当前前端实测默认的区别，未连接机器人或发送控制指令。
- **2026-08-06 11:57** 视频场景收束：SRS 以真实解码帧、Rosbridge/局域网以实际图像帧判定；连续 2.5 秒无新帧时保留最后画面并标记 `FRAME HOLD`，新增来源/分辨率读数与诊断状态；未更改 SRS、MapHub、rosbridge 协议或发送机器人控制指令。同步 README、项目约定与 HUD 设计。
- **2026-08-06 11:32** 补齐点云保持态边界：空回波或筛选为空后断流仍显示 `FRAME HOLD`；切流清除旧流陈旧计时器，移动端收紧场景读数避免溢出；未新增后端调用或控制指令。
- **2026-08-06 11:28** 点云场景收束：区分首帧等待、空回波、筛选为空、连接错误与 `FRAME HOLD`；连续 2.5 秒无新帧时保留最后画面并在诊断显示帧龄，/scan 移除背景工程格、参考层改为感知环/边缘刻度/原点环；未新增后端协议、TF 融合或机器人控制。同步 README、项目约定与 HUD 设计。
- **2026-08-06 10:51** 地图恢复系统原生光标；新增可关闭的单格边界连续性修复、孤立噪点降权与 `4×` 静态底图轮廓采样，仅作用于离屏显示位图，不参与占据判断或导航；同步 README、项目约定和 HUD 设计。
- **2026-08-06 10:06** 地图与点云场景化：主舞台改为视频/态势/感知；地图以环境、行动、风险做纯显示后处理，点云 /scan 增本地感知参考环并默认隐藏工程辅助线；不改导航真值、不做 TF 融合。同步 README、项目约定与 HUD 设计；类型检查通过，仅检查本地登录门禁，未构建、登录或联调。
- **2026-08-05 23:00** 操作者面板：抽屉新增设置/操作者/诊断三标签；复用当前操作权与 `OperatorChanged` 状态，管理员在 4 秒二次确认后才可移交/回收，未新建 Hub；同步 README、项目约定和 HUD 设计，仅做静态开发检查，未启动服务、构建或机器人联调。
- **2026-08-05 22:47** 点云筛选管线优化：单帧固化筛选条件并以平方距离比较，筛后全量边界稳定相机/着色，LOD 保留首尾且仅上传可见属性区段；同步 README 与项目约定，仅做静态代码检查，未启动服务、构建或机器人联调。
- **2026-08-05 22:14** 点云单色快速路径：单色改用材质统一上色，跳过逐点颜色缓冲与距离计算；高度/距离着色保持逐点真值。同步 README、HUD 与项目约定；仅做 SFC 静态解析，未启动服务、构建或机器人联调。
- **2026-08-05 21:57** 地图图层时效呈现：远端动态图层陈旧后降至 34% 透明度，新帧立即恢复；静态底图/本地轨迹不降权，阈值变化仅补一帧绘制。同步 README、HUD 与项目约定；仅做 SFC 静态解析，未启动服务、构建或机器人联调。
- **2026-08-05 21:50** 地图矢量叠加层 LOD：全局路径/局部规划分别限至 4,000/2,400 显示点，保留分段断点与首尾；局部规划点标记改为批量填充。同步 README、HUD 与项目约定；仅做 SFC 静态解析，未启动服务、构建或机器人联调。
- **2026-08-05 21:29** 点云高频帧合并：同一显示帧仅消费最后一包，暂停/切流/重连/卸载撤销待消费帧；质量读数、诊断与导出元数据统一对应实际呈现帧。同步 README、HUD 与项目约定；仅做 SFC 静态解析，未启动服务、构建或机器人联调。
- **2026-08-05 21:06** 点云收口：`/scan` 自动俯视、`points2` 自动三维，手动视角标记 CUSTOM；新增距离/高度筛选、原始/有效/筛后质量读数、PLY/CSV 元数据和静止按需绘制，诊断同步 DRAW IDLE；未启动服务、构建或机器人联调。
- **2026-08-05 20:50** 地图高频图层收口：静态地图、全局/局部代价图与地图增量改为独立脏标记并在单个动画帧合并重建；扫描/粒子维持均匀抽样上限，卸载取消待处理帧；同步 HUD、README 与项目约定，未启动服务、构建或机器人联调。
- **2026-08-05 20:41** 地图收尾首轮：遥测明确 ODO/SAFE 与 CMD 语义，新增驾驶/调试图层预设，并拦截静态占据栅格内的明显目标落点；同步设计与项目文档，未启动服务或机器人联调。
- **2026-08-05 20:17** 键盘转向改为 0.1→0.5 rad/s 速度斜坡，Shift 平滑趋近 1.0 rad/s；WASD 组合持续输出双轴 Twist，已同步控制文档，未启动服务或机器人联调。
- **2026-08-05 20:01** 修正 WASD 行进转弯混控：`WA / WD / SA / SD` 角速度固定 `±1.0 rad/s`，Shift 仅提升线速度；同步控制契约文档，未执行构建、开发服务器或机器人联调。
- **2026-08-05 19:34** 补齐 WASD 双轴组合与抵消输入门控，P3j 首阶段接入屏幕操作光标、世界指示标和短时落点反馈；同步 README、HUD 与项目约定，未执行构建、类型检查、开发服务器或机器人联调。
- **2026-08-05 14:37** 地图平移改为按住中键拖拽：左键只保留点选与长按框选，取消左键快速拖动平移；画布仅在中键按下时显示抓手状态。同步 README、HUD 与项目约定；未执行构建、类型检查、开发服务器或机器人联调。
- **2026-08-05 11:47** 实现地图初始交互：初始位姿提交后需人工确认才解锁导航，失权自动复位；桌面左键选中/平移/长按框选，右键单目标与方向拖拽，Shift 仅本地草案；新增地图范围、权限、MapHub、单一提交与重复目标门控，以及画布准星、选中角标和一次性落点环。同步 README、HUD、接口与项目约定；按当前约定未执行构建、类型检查、开发服务器或机器人联调。
- **2026-08-05 10:44** 修复 MapHub 地图帧恢复：自动重连保留已验证静态地图与 `CACHED` 指标，首次/重连后 1.5 秒无 Map 仅补订阅一次；Map 二进制解码错误进入地图与诊断抽屉，前后端 SDK 副本、README、接口文档和项目约定同步，未执行测试或构建。
- **2026-08-05 10:09** 认证请求超时从 8 秒提高至 60 秒：局域网 admin 首次实测 `200 / 21.882s`，浏览器同机制 30 秒复测仍超时、60 秒复测 `200 / 22.446s`；公网同接口 `200 / 102ms`，且 Swagger 路由暴露不同，不能视为同一认证环境。同步 README 与项目约定，未执行构建。
- **2026-08-05 09:45** 修复 Nuxt Icon 首屏告警：本地预打包 `lucide:eye`、`lucide:eye-off`、`lucide:log-in` 并开启静态扫描，避免 SSR 退回运行时图标请求；同步 README 与项目约定。`pnpm typecheck` 通过，开发页新请求无对应告警，未执行构建。
- **2026-08-04 20:03** 新增会话级 UI 状态保持：抽屉页签、地图图层和点云首选流/视觉参数可在舞台切换后恢复，点云显示上限按流能力钳制；地图图层新增新鲜/陈旧/缺失状态并与诊断中心共用阈值；`pnpm typecheck` 与开发页切换/刷新验证通过，未执行构建。
- **2026-08-04 19:15** 控制台抽屉新增设置/诊断双模式与无凭据 JSON 快照；地图增加跟随、600 点轨迹、图层数据状态和前端目标估算；点云增加均匀 LOD、三种着色、点大小、辅助线、冻结、数据/GPU FPS 及 PLY/CSV 导出；类型检查和 1280×720 开发页/Canvas 像素检查通过，未执行构建，动态实帧留待手动验收。
- **2026-08-04 17:17** 修复空闲手动速度源覆盖 Nav2：CommandHub 改为仅在 WASD / 摇杆有效输入时启动 15Hz 循环，最后输入释放或手动驾驶失焦时单次停车，纯导航、初次连接及无手动输入的连接回收不再注入零速；类型检查通过，管理员空闲 4 秒实测 `/manual_cmd_vel` 为 0 条消息，未执行构建。
- **2026-08-04 16:08** 点云默认流改为机器人 `/scan`，缺失时依次回退 `points2` 与后端首个流，保留抽屉手动切换并同步相关文档；未执行构建。
- **2026-08-04 15:00** 完成分层连通验证：MapHub 实收 169×93 地图、里程计与电池后又因后端 5122 超时以 WebSocket 1006 断开，机器人 9091 可持续直连且 9090 曾拒绝后恢复，激光与 points2 暂无源帧；地图新增浏览/目标/初始位姿三模式、拖动定向预览、确认取消、操作者权限门控与 HubException 反馈，局域网摄像头默认端口及文档同步为 9091，类型检查通过且未执行构建。
- **2026-08-04 13:18** 接入实时视频 FPS（真实媒体帧统计）、MapHub 地图画布（栅格/位姿/路径/扫描/代价图/图层工具）与 PointCloudHub 点云首帧状态；修正扫描离散点、地图图层按钮宽度和点云切流首帧竞态，完成 README / HUD / 项目约定同步。
- **2026-08-03 23:58** 沉浸化右缘媒体抽屉：收起静止态去除白色卡壳和模糊，仅保留 38px 透明导轨；悬浮/聚焦加入导轨延伸、状态点光晕与箭头位移，展开改为 340ms 横向 + 纵向双轴揭示、内容错峰入场和一次性顶部主色线；悬浮、聚焦及展开期间暂停闲时隐藏，并完成桌面/390px 开发态边界检查和文档同步。
- **2026-08-03 23:28** 沉浸化左上退出与右上 PiP：退出按钮改为无底色箭头 + 对角开放角线，保留 40px 命中区、Tooltip 与按压反馈；PiP 切换为 `HudPanel bare` 四角取景框，移除 `.hud-panel-bd`、玻璃背景与模糊，保留点阵、互换提示和 reduced-motion 降级；同步 README、HUD 设计契约与项目约定。
- **2026-08-03 23:08** 顶部 ODO 遥测带接入 MapHub Odometry / SafeTwist 真值，增加 10Hz 合帧、2.5 秒陈旧值清理、固定宽数值微动画与断线占位，并同步明日验收清单及地图/导航/诊断后续策划。
- **2026-08-03 22:33** 接入 SRS WebRTC、Rosbridge CameraImage 与局域网直连三种视频来源，修正内网 SRS 回环签发地址、无媒体帧时的 `LIVE` 误判及移动端抽屉裁切/透底；视频/点云设置迁入右侧玻璃抽屉，HUD 闲时改为完全隐藏，并同步配置、联调结论与文档。
- **2026-08-03 21:48** 响应式拆分控制输入：桌面端仅显示 WASD/Shift/Space 键帽映射，接入真实按压、速度档位和急停脉冲反馈；`≤720px` 移动端仅显示摇杆与急停，并同步 README 与 HUD 设计文档。
- **2026-08-03 20:56** 新增临时局域网摄像头源：前端可在 WHEP 与 rosbridge 间分段切换，直连 `ws://192.168.1.30:9090` 订阅 `/aurora/rgb/image_raw`，支持 rgb8/bgr8、自动重连、帧超时和连接释放；实测收到 320×200 BGR8 帧，类型检查及桌面/390px 开发态切换通过，后端当前超时故登录后画面留待手动验证，未执行构建。
- **2026-08-03 20:17** 平滑电量并打通局域网视频：BatterySoc/BatteryVoltage 增加 5 点中值、指数低通与显示死区，现场原始 SOC 在 46/47 间波动时页面连续采样稳定为 47%；视频不再被失真的 `isOnline=false` 提前拦截，WHEP 失败时使用 MapHub `CameraImage`（320×200 BGR8，约 7 FPS）Canvas 备用画面并保持 `stream.play` 门控；直播态接入边缘明度幕和浅色 HUD。确认 SRS `live/demo-01` 正在推流，但 WHEP 仍无法访问，问题位于公网 SRS Hook/ICE 网络层而非 SignalR；`pnpm typecheck` 通过，未执行构建。
- **2026-08-03 18:13** 修复离线视频舞台可读性：离线、连接中和错误状态改为白色背景，仅 RTC 播放成功后使用深色视频背景；新增 MapHub 电池遥测，登录后订阅 `BatterySoc` / `BatteryVoltage`，SOC 按 0-100 钳制并在断线时清空旧值；电量条接入真实填充、百分比/电压切换、高光扫过、低电量警示与 `prefers-reduced-motion` 降级。类型检查已通过；开发态确认白色背景、HUD 可读性、无横向溢出及无数据占位，局域网后端当前超时，真实 SOC/电压留待手动联调。
- **2026-08-03 17:36** 接入视频与点云舞台：视频按设备列表和 `stream.play` 权限创建 WHEP Player，支持切换、重连与释放；点云通过 PointCloudHub MessagePack 写入 Three.js 动态缓冲，支持 `points2`/`scan`、点数与视角适配，新增 VID/PCD 真值状态；共享媒体工具条样式并修复 390px HUD 重叠，开发态确认游客视频门控、两个离线设备、点云 Hub 连接及桌面/手机非空画布，在线视频与真实点云帧留待手动联调。
- **2026-08-03 17:08** 调整验证流程：停止遗留的 Nuxt 构建进程，将中断生成的 `.output` 移出工作区；后续默认只做开发态检查与必要类型检查，不再自动执行生产构建，最终功能由用户手动验收。
- **2026-08-03 16:51** 完善账户认证：新增登录/注册弹窗、邮箱验证与重发、游客登录、退出、忘记/重置密码、401 与 Token 到期清理，Access Token 仅存页面内存；移除开发期 admin 自动登录，CommandHub 改为真实会话 + `isSelf` + OperatorChanged 门控，游客和无权账户禁用控制；W/S 常规档改为 0.5 m/s、Shift/摇杆满量程请求 1.0 m/s（后端当前仍钳制 0.5，待同步配置）；视频、点云与遥测等待手动验收后再接入。
- **2026-08-03 16:33** 局域网联调完成：前端固定从 `http://localhost:5173` 直连 `http://192.168.1.139:5122`，匹配后端 CORS 并支持 SignalR WebSocket，可用 `NUXT_BACKEND_BASE_URL` 覆盖；TypeScript 固定为兼容 vue-tsc 的 6.0.3，修复控制台舞台回退值的严格空值类型；同步 README、项目约定、后端接入文档、HUD 设计文档、Postman Local View 与本地检查配置，将错误启动生成的 `Frontend/--host/` 缓存移出工作区；类型检查、生产构建及真实浏览器 REST/SignalR 联调通过。
- **2026-08-03 08:23** 驾驶手感层：键盘映像摇杆（WASD → 旋钮弹向指令矢量，半速半程 / Shift 满程，力度环随亮；拖拽优先）；Space 急停同步触发急停钮按压回弹；方向倾斜（驾驶时世界层反向平移 6px + 转向侧倾 ±0.8°、HUD 顺向 2px 惯性，与鼠标视差经 quickTo 合成，急停/松键复位归零）。
- **2026-08-02 22:13** P3a 移动接入：后端 SDK 拷入 `app/lib/cwebapi/`，装 `@microsoft/signalr@8.0.7`（+msgpack）并以插件挂载 `globalThis.signalR`；新增 `useRobot` 组合式（开发期 admin 自动登录（5s 超时）→ 操作者校验 → CommandHub 15Hz 速度循环）；键盘控制 WASD（半速 0.25）/Shift（满速 0.50）/Space（急停清输入），摇杆模拟量并轨（force>0.05 优先）；失焦/隐藏页立即停车；生命体征簇状态点/操作者名/速度全部真值化，后端离线时如实转灰、本地反馈不受影响；nuxt devProxy 同源转发规避 CORS。设计文档新增 §5.5 控制输入规范，P3 分解为 a–f（登录 UI 最后）。
- **2026-08-02 21:59** 实现 HUD P2 微交互层：急停×摇杆合并为「控制舱」（CTRL 标签 + 结构线相连、命中区分离）；摇杆改原生 Pointer Events（力度→刻度环/力度环渐显→生命体征簇速度实时联动，松手弹性回中，nipplejs 留作备用）；闲时隐退/唤回（3s 无输入淡至 0.15 并退向边缘，急停保持 0.6，任何输入 250ms 唤回）；急停触发反馈（斜纹环转圈 + 红晕双脉冲 + 速度归零）；PiP 与主舞台点击互换（与切换器联动去重）；鼠标视差（HUD ±3px / 舞台 ∓6px 双层深度）；慢机型保险（进场未完成时按下即快进完成）。
- **2026-08-02 21:21** 实现 HUD P1.5 透明化重构：全区块去卡壳（HudPanel 增 bare 变体，玻璃仅剩 PiP/抽屉 2 块）；新建左下生命体征簇（ADMIN+三连接点 / 236×6px 切角刻度电量条 / 电压+速度行），删除旧顶部状态条；遥测带迁顶部居中、切换器改清单式（2px 主色键线）、摇杆移急停上方中心对齐；进控制台收起 nav/footer（320ms 收回→装配进场），Esc + 左上 Mio 微钮退出；修复 `--hud-bar-w/h` 令牌缺失。
- **2026-08-02 21:06** `console-hud.md` 升级 v3（策划，未动代码）：从玻璃卡片转向 FPS 式透明叠印（卡壳只留 PiP/抽屉，玻璃预算 6→2）；新增左下「生命体征簇」规范（身份行 + 236×6px 切角电量条 + 数据行，8px 网格）；进控制台收起 nav/footer（剧场模式并入默认），Esc + Mio 微钮退出；词汇表增 D+（FPS 连续条）、K（边缘明度幕）、J′（清单键线）；排期插入 P1.5 透明化重构。
- **2026-08-02 20:48** 实现控制台 HUD P1 静态骨架：8 区块全部落位（状态条/主舞台/视图切换/PiP/摇杆盘/遥测条/急停/抽屉把手），词汇表 A–F 落地（HudPanel 双层裁切切角+1px 边线、悬停角括号、摇杆 30° 刻度、10 格电量、英文微标签、点阵空态），装配式进场时间线（取景括号描边→面板错峰滑入→微标签→连接点→扫描线）；已决：急停大圆钮、微标签英文、夜间/剧场模式排期 P5/P2。全部假数据。
- **2026-08-02 20:35** `console-hud.md` 升级 v2：确立「三层阅读距离」（远看极简/中看仪器/细看游戏），新增游戏化装饰词汇表 A–J（切角、角括号、刻度、分段仪表、微标签、点阵、扫描线、准星、警示斜纹、选中键线）及密度预算（每面板 ≤2 种、透明度 ≤0.25、禁循环动画），进场改为「装配式」编排。
- **2026-08-02 20:28** 新增 `Design/console-hud.md`：控制台 HUD 设计方案 v1（白色系毛玻璃、8 区块布局、玻璃令牌、微交互目录、动效编排、性能红线、P1–P4 实施阶段）。
- **2026-08-02 15:33** 搭建控制台框架：nav-links 切换概览/控制台视图（渐变过渡，概览留空、控制台暗色占位）；引入 Tailwind v4 + shadcn-vue（Reka UI，手写 Button）、Three.js、nipplejs、@nuxt/icon（lucide + game-icons 本地图标集）；设计令牌迁至 `app/assets/css/main.css`（命名对齐 shadcn，含暗色主题占位）；`lucide-vue-next` 已废弃换用 `@lucide/vue`；新增 `components.json` / `tsconfig.json`；pnpm 拒绝 `vue-demi` 构建脚本。
- **2026-08-02 15:12** 确立动画方案并接入：安装 `gsap@3.15` / `motion-v@2.3`；首页入场编排（GSAP 时间线：导航滑入 + 子项错峰淡入 + 页脚淡入，尊重 `prefers-reduced-motion`）；登录按钮改用 motion-v 弹簧悬停/按压。
- **2026-08-02 14:51** 手动微调：站点标题改为 `ZeroRobot`，导航品牌名改为 `Mio`，栏目精简为「概览 / 控制台」。
- **2026-08-02 14:27** 创建前端首页骨架：顶部导航（nav-left：品牌 + 栏目链接；nav-right：游客访问 + 登录）、空内容区 `.zero`、空页脚 `.site-footer`，视觉按 `Design/modern.md`；`nuxt.config.ts` 增加站点 head 配置；新建 `README.md` / `CLAUDE.md` / `zero.md`。
